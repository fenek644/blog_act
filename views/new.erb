<h2>Add New Post form </h2>

<p>
<form action="/new" method="POST">
  <div class="form-group">
    <label>Ваше имя</label>
    <input name="post[author]" type="text" class="form-control"  placeholder="Введите имя" value="<%= @p.author %>">
  </div>
  <div class="form-group">
    <label>Введите текст поста</label>
    <textarea name="post[content]" class="form-control" placeholder="type your post here" rows="6"><%= @p.content %></textarea>
  </div>

  <button type="submit" class="btn btn-primary">Submit</button>
</form>
</p>